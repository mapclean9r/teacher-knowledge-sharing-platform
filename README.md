# teacher-knowledge-sharing-platform

about -> todo

### Config/setup

Setup env files -> click `Run`, `Edit Configurations`, then select `application`,
choose the main file (TpModuleApplication.java), then add the following to your environment `DB_URL=jdbc:postgresql://localhost:5432/myapp;DB_USERNAME=postgres; DB_PASSWORD=1234`.
Change the information to your database information.